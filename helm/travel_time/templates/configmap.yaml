apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "travel-time.fullname" . }}-config
  labels:
    {{- include "travel-time.labels" . | nindent 4 }}
data:
  {{- if .Values.batching.enabled }}
  batching.config: |
    max_batch_size { value: {{ .Values.batching.maxBatchSize }} }
    batch_timeout_micros { value: {{ .Values.batching.batchTimeoutMicros }} }
    max_enqueued_batches { value: {{ .Values.batching.maxEnqueuedBatches }} }
    num_batch_threads { value: {{ .Values.batching.numBatchThreads }} }
  {{- end }}
  {{- if .Values.monitoring.enabled }}
  monitoring.config: |
    prometheus_config {
      enable: true,
      path: "/monitoring/prometheus/metrics"
    }
  {{- end }}
